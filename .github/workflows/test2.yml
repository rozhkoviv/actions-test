  name: Test2

  on:
    repository_dispatch:
      types:
        - stage_tests
        

  jobs:
    run_test:
      runs-on: ubuntu-latest
      steps:
        - name: Tests start
          run: |
            echo ${{ toJson(github) }}
